# This is an example environment file. Copy it to .env and fill in the values.
# For the default Docker setup, these values do not need to be changed.
DATABASE_URL=postgresql://postgres:postgres@db:5432/roadmapdb
